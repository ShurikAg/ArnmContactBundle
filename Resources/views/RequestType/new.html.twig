{% extends 'ArnmAraneumBundle::araneum.html.twig' %}

{% form_theme form 'ArnmAraneumBundle:Form:araneum.html.twig' %}

{% block sidebar %}
	<div class="alert alert-info hint">
		<span class="label label-info">{{ 'default.label.info'|trans }}</span><br/>
  		{{ 'contact.request_type.new.info'|trans({}, 'contact')|raw }}
	</div>
{% endblock %}

{% block title %}
	{{ parent() }} | {{ 'contact.request_type.form.title.new'|trans({}, 'contact') }}
{% endblock %}

{% block content %}
	<div class="page-header">
		<h2>{{ 'contact.request_type.form.title.new'|trans({}, 'contact') }}</h2>
	</div>
	
	{% include 'ArnmAraneumBundle:Misc:messages.html.twig' %}
	
	{% if form %}
		<form action="{{ path('arnm_contact_request_type_new') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
			<fieldset>
				{{ form_widget(form) }}
				<div class="form-group">
                    <div class="col-sm-offset-2 col-sm-10">
    			    	<input class="btn btn-primary" type="submit" value="{{ 'contact.request_type.form.action.create'|trans({}, 'contact') }}"/>
    			    	<input class="btn" type="reset" value="{{ 'default.form.action.reset'|trans }}"/>
    			    	<a  class="btn" href="{{ path('arnm_contact_request_types') }}">{{ 'default.form.action.back_to_list'|trans }}</a>
    			    </div>
			    </div>
		    </fieldset>
		</form>
	{% endif%}
{% endblock %}
